<h1>Profiles</h1>

<% @profiles.each do |profile| %>
    <div class="row">
      <div class="col-md-8">
        <%=
          link_to profile.user.fullname, profile_path(profile)
        %>
      </div>
    </div>
<% end %>