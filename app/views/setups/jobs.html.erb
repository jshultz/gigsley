<h1>Create/Update Your Profile</h1>
<div class="row">
  <div class="col-md-3 col-md-offset-9"><%= link_to "Looking for Help?", profile_index_path %></div>
</div>
<div class="row">
  <div class="col-md-3 col-md-offset-1">
    <div class="row">
      <div class="col-md-12 setupSidebarBorder">
        <div class="row">
          <div class="col-md-12">&nbsp;</div>
        </div>
        <%= render "setups/sidebar" %>
      </div>
    </div>

  </div>
  <div class="col-md-6">
    <div class="row">
      <div class="col-md-12 setupBorder">
        <div class="row">
          <div class="col-md-12">&nbsp;</div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <% if @user.profile.provider == true %>
                <p>Ready to find a job? Fill out basic account information to set up your account and get started. You can finish the rest of your profile later on.</p>
            <% else %>
                <p>Ready to find that person who will change your life? Just choose who you're looking for!</p>
            <% end %>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <%= simple_form_for :profile, url: {action: 'jobs'} do |f| %>
                <div class="row">
                  <div class="col-md-12">
                    <%
                      job_options = Job.all.map do |s|
                        [ s.name, s.id ]
                      end
                    %>
                    <label>Would you like to register as a? </label>
                    <%= f.select :job, options_for_select(job_options), {}, { required: 'true', autofocus: 'true', class: 'form-control'} %>
                  </div>
                </div>
                <div class="row">
                  <div class="col-md-12">
                    <%= f.input :skill_list, as: :string, label: "Skill List (comma separated)", input_html: {value: @user.profile.skill_list } %>
                  </div>
                </div>
                <div class="row">
                  <div class="col-md-12">
                    <%= f.submit %>
                  </div>
                </div>
            <% end %>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">&nbsp;</div>
        </div>
      </div>
    </div>
  </div>
</div>
