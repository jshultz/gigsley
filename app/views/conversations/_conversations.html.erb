<% @conversations.each do |conversation| %>
        <% read_class = conversation.is_unread?(@user) ? 'unread' : '' %>
        <%= render :partial => 'conversation', :object => conversation, :locals => {:read_class => read_class} %>
<% end %>